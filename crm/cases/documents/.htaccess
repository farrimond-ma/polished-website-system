# Generated client documents. They are served through document.php (which requires a CRM login),
# never by direct link, so direct web access is blocked.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
